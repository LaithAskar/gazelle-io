/**
 * Placeholder export for the approved Apify curriculum-ingestion package.
 *
 * The live MVP currently seeds Common Core curriculum directly; Apify scraping is
 * intentionally deferred until the architect approves source-specific jobs.
 * Keeping this module typed prevents the empty package from breaking monorepo
 * verification while preserving the locked package boundary.
 */
export const APIFY_SCRAPING_DEFERRED = true;
