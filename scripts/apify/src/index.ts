// Placeholder module so the Apify workspace has at least one TypeScript input.
// Real curriculum scraping jobs are intentionally added later only for approved
// open-source/public-domain sources listed in AGENTS.md.
export {};
