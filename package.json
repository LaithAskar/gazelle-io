{
  "name": "gazelle",
  "version": "0.0.0",
  "private": true,
  "description": "Gazelle.io — AI-powered adaptive learning platform for K-6 (monorepo)",
  "packageManager": "pnpm@11.4.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "type-check": "turbo type-check"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "turbo": "^2.5.0",
    "typescript": "^5.6.0"
  }
}
